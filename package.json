{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "music": "lerna run build --scope=@cloud/music",
    "git": "lerna run git --scope=@cloud/git"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^6.6.1"
  }
}
